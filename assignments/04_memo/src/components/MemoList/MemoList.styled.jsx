import styled from 'styled-components';

export const StWrapper = styled.ul`
    display: flex;
    flex-direction: column;
    border-right: 1px solid var(--border-color);
    overflow-y: auto;
    padding: 20px 12px;
    gap: 8px;
    height: 100%;
`;
